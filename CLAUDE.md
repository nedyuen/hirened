# HireNed.Now

@docs/requirements.md

Personal site selling Ned Yuen's professional experience to employers. Three pages, one Supabase backend, eventual multi-tenant SaaS. Full reasoning behind every data-model decision is in the imported requirements.md above — read it before making changes, not just this file.

## Current state (keep this section updated as work progresses)
- Supabase project "Hire Ned" is live, RLS enabled, seeded with real data (1 profile, 3 companies, 17 projects, 29 evidence rows) originally seeded with 5 roles, now 6 after adding the Accenture Internal/BD role directly (see below) — and **Explorer.jsx and Profile.jsx now query it live** — only Interview.jsx still has no backend. `src/lib/supabase.js` exports the one shared client (`createClient(import.meta.env.VITE_SUPABASE_URL, import.meta.env.VITE_SUPABASE_ANON_KEY)`), anon-key/RLS-scoped for reads, used by both pages.
- **Schema simplified after seeding** (some originally-designed fields dropped as unused/premature — this happened outside a Claude Code session, discovered via direct introspection when wiring the frontend): `evidence.related_project_id`, `evidence.year`, `evidence.is_sample`, `evidence.audience_tags` are all gone, and so are the `responsibilities` and `audiences` tables entirely (5 tables now, not 7). `projects.year` was added to replace `evidence.year` (one project = one year, not per-evidence) but was left NULL after the migration — backfilled via `scripts/backfill-project-years.mjs`, which extracts the original per-project years out of Explorer.jsx's (since-removed) hardcoded `PROJECTS` data the same way `seed.js` does, using the earliest year where a project had multiple evidence years (none currently do). *(This has since changed again — `projects.year` was itself replaced by `start_date`/`end_date` and dropped; see the dedicated entry below.)* The one evidence row that used `related_project_id` (Digital & Platform Services Excellence Award, previously role-level with a "references but doesn't own" link to Data Science Solutions) is now directly `project_id`-owned by that project with `level: 'project'` — the reference-without-ownership pattern described in requirements.md's data model no longer exists in the schema. **Content nuance, now fixed:** this award's `detail` text used to say "Logged at role-level... since the award recognised the broader initiative... not this project alone" — inconsistent with its `level: 'project'`/`project_id` assignment. Updated directly in Supabase (one-row content fix, not a schema/code change) to describe the award's scope without the stale role-vs-project framing, without just restating `bullet` (the non-negotiable `detail` ≠ `bullet` rule still applies).
- **`resolveRole()` is deleted, not just fixed.** Its whole job (guessing which role/stint a piece of evidence belongs to, from year + client) is now unnecessary: every `evidence` row has a real `role_id` FK (or reaches one via its `project_id` → `projects.role_id`), set correctly once during seeding. Do not reintroduce year/client-based role-guessing against live data — if a future evidence row needs a different role, fix its `role_id` directly.
- **Explorer.jsx now fetches from Supabase** via `fetchExplorerData()` (a module-level async function, called from a `useEffect` on mount) — one transform layer that fetches `companies`/`roles`/`projects`/`evidence` and reassembles the exact same flat row shape the filtering/grouping/search/rendering code already expected (`company`, `employer`, `jobTitle`, `functionalRole`, `project`, `description`, `bullet`, `detail`, `metric`, `title`, `quote`, `source`, `type`, `level`, `tags`, `highlighted`, `sort_order`, `placeholder` — `description` was `goal` until the `projects.goal` rename, see the dedicated entry below), so none of that downstream logic had to change. Shows a plain "Loading evidence…" message while the fetch is in flight. `TARGET_ROLES` stays a hardcoded module const, deliberately not migrated (see requirements.md §6). Default sort is now `sort_order` ascending (preserves seeded/authored order) — `year` no longer exists on evidence to sort by. **The audience/tech-banking lens system was removed along with it** (`lens` state, `?audience=` param, the "Audience: X" active-filter pill, `TARGET_ROLES[name].audience` no longer consumed) — `evidence.audience_tags` doesn't exist in the schema anymore, and this already matched the documented "single lens throughout" decision (requirements.md §1) that the code just hadn't fully caught up to. The `relatedProject`/"↳ relates to X" card footer is also gone, matching the schema simplification above.
- **Profile.jsx now fetches from Supabase** too: `CAREER_HIGHLIGHTS` and `TESTIMONIALS` keep their same hand-maintained shape (category/label → a title or project-name reference) but resolve the actual `bullet`/`goal`/`quote`/`source` text live by querying `evidence`/`projects` on mount, instead of the old local `HIGHLIGHT_TEXT`/`TESTIMONIAL_TEXT` hardcoded copies (which are deleted). `TESTIMONIALS` entries dropped their hardcoded `source` field too, since `source` is now resolved live alongside `quote` — the mapping is just `{ title }` now. The Career Journey timeline (`TIMELINE` array, previously hardcoded with a `jobRole` override field) is also gone — timeline entries now come from a live `roles` (joined to `companies`) query, so the displayed role text and the Explorer link target are always the same value (no override needed anymore). One side effect: the timeline's descriptive blurb per role now reads as `roles.description` (matching what the Explorer's role-group header shows) rather than the old Profile-specific phrasing — slightly different wording, same facts, and now genuinely one source of truth instead of two. *(This has since changed again — see the `roles.team` entry below; the timeline no longer shows `description`.)* Title/name-string matching (not a stable ID) remains a known limitation for both `CAREER_HIGHLIGHTS`/`TESTIMONIALS` — unchanged by this, still worth a stable-ID pass eventually.
- `<a href>` used for cross-page nav — should become React Router `<Link>` eventually.
- Interview.jsx is an intentional placeholder — do not build it out unless explicitly asked.
- The Company→Role→Project node-link tree diagram was removed from Explorer.jsx's filter panel (too much vertical space for how rarely it was used) and replaced with flat multi-select chip groups for Company, Role, and Project, matching the existing Delivery role/Expertise/Type chip pattern.
- Explorer's URL params: `company` sets the Company filter; `jobRole` (paired with `company`) sets the job-title Role filter; `role` is reserved for the Delivery role filter — these are deliberately distinct params, don't conflate them (see "Role vs Delivery role" below).
- Explorer's `groupBy` and `filtersOpen` are now both URL-param-aware (previously hardcoded to `"company"` / `true`): `groupBy` reads `?groupBy=`; `filtersOpen` defaults to collapsed when landing with any of `type`/`cat`/`role`/`tier`/`preset`/`company`/`q`/`groupBy` present (arriving pre-filtered from Profile), expanded otherwise (direct nav). Don't accidentally revert these to hardcoded defaults.
- Fixed a latent gap while wiring this up: `?preset=` previously only set the `activePreset` label/pill on load — it never actually applied that preset's `categories`/`deliveryRole` to the real filter state, so a preset link filtered nothing. `selectedCats`/`selectedDeliveryRoles` now also seed from `TARGET_ROLES[preset]` on mount (only when the more-specific `cat`/`role` params aren't already present) so Profile's "Roles I'm looking for" chips (`explorerLink({ preset: roleName })`) actually filter on arrival, not just label the pill.
- Profile has a Testimonials section (between Education & Qualifications and the footer CTA), showing 2 reviews via a hand-maintained `TESTIMONIALS` mapping (title-only refs, resolved live — see above). Both source reviews are currently placeholder content in the underlying data — swap in real reviews during the broader content-review pass (~14 placeholder projects, see requirements.md §9).
- **Profile.jsx restructured (multiple changes in one pass):**
  - Nav now has a filled "Contact Me" button (top-right, `mailto:nedyuen@gmail.com`) — placeholder for a future purpose-selector modal (Request CV / Discuss an opportunity / Just want to connect), not built yet.
  - Hero's `SUMMARY` is now 2 prose paragraphs, not a bullet list. `CONTEXT` tags dropped "Divisions" (Industries/Companies/Functions remain).
  - "Roles I'm looking for" moved out of the Reasons-to-Hire section into the hero itself (right after the summary paragraphs, before the context tags) — behavior unchanged, still links via `explorerLink({ preset: roleName })`.
  - Personality and Work Style are now one merged section (eyebrow "Personality & Motivators", title "What am I like to work with?" — see heading-scheme note below) with a two-tab toggle (`aboutTab` state, default `"personality"`) instead of two separate sections.
  - **Strengths section removed entirely** (`STRENGTHS` array, `openStrength` state, and its click-to-expand rendering all deleted) — judged duplicative with Reasons to Hire.
  - **The compact Company→Role diagram in Career Journey was removed** (`COMPANIES_ROLES` const deleted). The timeline itself is now the link surface instead: each entry's role text and company text are independently clickable `<a>` tags (role → `explorerLink({ company, jobRole, groupBy: "role" })`; company → `explorerLink({ company, groupBy: "company" })`), styled with underline-on-hover. (The timeline's data source has since changed again — see the Supabase-live-query entry above; the link mechanism itself is unchanged.)
  - Footer gained email (`mailto:`) and LinkedIn links. LinkedIn now points to Ned's real profile (`https://www.linkedin.com/in/nedyuen/`) — the earlier placeholder URL has been filled in.
  - Section order (top to bottom) is: Reasons to Hire, About Me (merged Personality/Work Style), Expertise, Career Journey, Education & Qualifications, Testimonials — shifted after Work Style's standalone section merged in and Strengths was removed outright.
- **Profile.jsx heading scheme changed**: every section's `SectionHeader` now uses eyebrow=short plain label, title=bold question (e.g. eyebrow "Why Hire Me" / title "Why should you hire me?" — full eyebrow/title pairs for all 6 sections are in requirements.md §3). The hero's small label above the headline now reads "Professional Summary" (was "Career Profile") — label change only. **Reviewed and confirmed:** all 6 sections use the question-title format even though 5 of the 6 share a near-identical "What is/are/am I ___?" template back-to-back — Ned looked at the rendered page and explicitly chose to keep it as-is rather than vary the phrasing or revert some to plain labels. Don't re-relitigate this without new input from Ned.
- **Hero H1 headline changed**: was "Senior Strategy & Transformation Lead, 13 years inside Capital Markets.", now "Senior Strategy and Transformation Lead — driving operational efficiency through AI, Product & Program Delivery." — a content change requested directly by Ned, not a styling pass. (Older notes describing the H1 as "unchanged" predate this and no longer apply.)
- **Numbered-prefix eyebrows are back** ("01 — Why Hire Me" through "06 — Testimonials"), reversing an earlier decision to remove them. They were originally dropped while sections were still being reordered/removed frequently (numbering churn wasn't worth tracking); now that the 6-section order is stable, Ned asked for simple sequential numbers prefixing the existing eyebrow label text itself (no `SectionHeader` component change — just the string passed to `eyebrow`). Hero's "Professional Summary" label is unnumbered (Hero doesn't use `SectionHeader` and was never in scope). If sections are reordered or added again, renumber these six prefixes to match.
- **Profile.jsx hero now has a headshot** (`Headshot` component, next to the eyebrow/headline). Points at `/headshot-placeholder.jpg`, which **does not exist yet** — Ned needs to supply a real photo at `public/headshot-placeholder.jpg`. Falls back to a circular "NY" initials avatar (ACCENT-colored) via an `onError` handler if the image 404s, so the hero doesn't visibly break in the meantime. (Note: the task description that prompted this referenced an existing "NY avatar treatment used elsewhere in the app" — no such treatment actually existed anywhere in the codebase; this fallback is a new, one-off component, not a reuse of prior art.)
- **Accenture split into two job-stint entries**: "Consulting Manager — Morgan Stanley secondment" (2017–2019) and "Consulting Manager — Goldman Sachs secondment" (2019–2021), replacing one combined "Consulting Manager (2017–2021)" stint — now living in the `roles` table (seeded from what was originally a hardcoded `ROLES` array in Explorer.jsx and a `TIMELINE` array in Profile.jsx, both since replaced by live Supabase queries, see above). Full reasoning and the (inferred, unconfirmed) date boundary are in requirements.md §8. The client-vs-year role-resolution bug this split originally surfaced (a Goldman-Sachs-attributed item resolving to the Morgan Stanley stint) was fixed correctly once at seed time — see the `resolveRole()` deletion note above for why this can't recur against live data.
- **A third Accenture role exists: "Consulting Manager"**, the general/parent role (2017-09 – 2021-08, spanning the full Accenture tenure), with two client-secondment roles nested underneath it as children — currently titled "Goldman Sachs secondment" (2019–2021) and "Morgan Stanley secondment" (2017–2019) in the database (Ned shortened both from their original "Consulting Manager — X secondment" form directly in Supabase, since repeating "Consulting Manager" in front of an already-nested child read as redundant). This went through three iterations, worth knowing the history of in case old context resurfaces:
  1. First built as a role named "Consulting Manager — Internal / Business Development", inserted via a one-off script (service_role key from `.env.local`, not committed) to cover internal/non-client work spanning the full tenure plus a ~2-month pre-secondment gap Morgan Stanley's start (2017-11) didn't cover. Its date range deliberately overlapped both secondments. The Career Journey timeline rendered it as a *flat* entry (same level as the two secondments) with a muted/dashed "Concurrent" badge treatment to signal the overlap was intentional, not a bug.
  2. Renamed from "Consulting Manager — Internal / Business Development" to plain **"Consulting Manager"** (its genuine general title — dates unchanged) and reframed as the real top-level parent, with the two secondments nested underneath as children — not siblings under a computed/synthesized parent. There is no synthesized parent row; "Consulting Manager" is a real `roles` row with its own `role_id`, and the 3 evidence rows already on it ("Scrum, DevOps & ML analytics adoption", "AI-based PM tool — Innovation Challenge winner", "Leadership DNA (Innovate) Award") stayed attached, no migration needed. The nesting itself was inferred client-side by `job_title` **prefix** matching (child's title had to literally start with `"${parent.job_title} — "`).
  3. **That prefix-matching broke** the first time Ned shortened the secondment titles directly in Supabase (dropping the "Consulting Manager — " prefix, see above) — the timeline silently reverted to showing all Accenture roles flat, no error, just quietly stopped nesting. **Fixed by switching the nesting rule from title-text matching to date-range containment**: a role nests under another same-company role whose `[start_date, end_date]` range strictly contains its own (open-ended/ongoing roles treated as ending far in the future for this comparison only). This reflects the actual real-world relationship (a secondment is a sub-period of the general tenure) and is now immune to rewording either role's `job_title` or `description` in Supabase — only changing the *dates* so a secondment's range escapes its parent's range would break it again.
  - **Career Journey timeline nesting (current, `Profile.jsx`):** the fetch effect computes `containers = items.filter(p => p.org === item.org && p.start <= item.start && p.end >= item.end && (strictly larger))`, then nests under the tightest (smallest-range) container found, if any — a generic, non-hardcoded rule; `roles` has no self-referencing parent column in the schema, so this is inferred at render time regardless of method. Top-level entries (JP Morgan, "Consulting Manager", both UBS roles) render with the original flat treatment — solid dot marker, full-size text. Children (the two secondments) render nested inside the parent's content column, indented with a `border-l-2` rule and smaller text. The top-level "Consulting Manager" row links via the same generic `explorerLink({ company: t.org, jobRole: t.role, groupBy: "role" })` every top-level entry uses — no special-casing needed. Each child link is independently generated the same way, using whatever `job_title` currently lives in Supabase.
- **`roles.team` column added** (`alter table roles add column team text;`, run by Ned directly in the Supabase SQL editor — no Postgres connection or MCP access was available this session to run DDL, only the service_role key via PostgREST, which can't execute arbitrary SQL). Structural/org counterpart to the already-correct `roles.description` (narrative) — see requirements.md §2 for the full "structural vs. narrative" reasoning, same shape as other two-facet splits in this schema. `roles.team` is nullable and currently empty for all 6 roles; Ned will populate it directly in Supabase. **Profile.jsx's Career Journey timeline now shows `roles.team` instead of `roles.description`** (both the top-level query and the `t.d`/`c.d` fields feeding the timeline rows/children) — conditionally rendered (`{t.d && <div>...}`) so a null `team` omits the line entirely rather than leaving an awkward blank gap. **Explorer.jsx's group-by-role header is unchanged** and still shows `roles.description` — verified by filtering to Accenture/Consulting Manager post-change, header text still reads the narrative blurb, unaffected by the Profile-side switch. Note: a `projects.origin`/`projects.significance` split was referenced once as if already built (by the person requesting this `roles.team` work) — confirmed via direct schema check that it did **not** exist at the time. *(A `projects` narrative split has since been built — see the dedicated entry below — but named `description`/`detail`, not `origin`/`significance`.)*
- **`projects.year` replaced with `start_date`/`end_date`**, matching `roles`' existing date-range shape (`end_date = null` = ongoing/current, same convention). This session had live Supabase MCP access (reconnected mid-session — earlier sessions only had the service_role/PostgREST key, which can't run DDL), so the migration ran directly: `alter table projects add column start_date date; alter table projects add column end_date date;`, then a backfill (`update projects set start_date = make_date(year, 1, 1) where year is not null;`, `end_date` left null for every row), verified against all 17 rows, then `alter table projects drop column year;`. **The backfilled `start_date` values are a placeholder, not researched data** — Jan 1 of whatever `year` used to hold, same caveat as the (also unconfirmed) Accenture secondment date boundary; Ned should correct real start/end dates directly in Supabase. **No frontend code changed** — searched Explorer.jsx and Profile.jsx thoroughly first (not just grepped for `year`, read `fetchExplorerData()`'s full transform in full) and confirmed `projects.year` was never actually read anywhere: not sorted on (that's `sort_order`), not displayed on any card, not used in `CAREER_HIGHLIGHTS` lookups. It was only ever pulled in incidentally by `select("*")` calls and discarded before reaching any rendered field, so dropping the column was a zero-code-change, zero-risk operation — verified by loading both Explorer.jsx (grouped by every dimension) and Profile.jsx post-drop with no console errors and full data intact.
- **`projects.goal` renamed to `projects.description`, plus a new nullable `projects.detail` column** — `evidence.bullet`/`evidence.detail` pattern applied one level up the hierarchy. Migration ran directly via MCP: `alter table projects rename column goal to description; alter table projects add column detail text;`. `detail` is deliberately **not backfilled or invented** — same as `roles.team`, Ned writes it directly in Supabase over time; verified all 17 rows still show `detail: null` after the migration. Every `goal` reference across both files was renamed to `description` (not just the DB column — the JS-side field names too, for consistency): in Explorer.jsx, `fetchExplorerData()`'s `projects` map and both `fromProjects`/`fromRoles` evidence-row builders, the search haystack, `goalByKey` renamed to `descByKey` (plus a new parallel `detailByKey`), the project filter chip's tooltip, the group-by-project header, the placeholder-card display, and the "Project context" box; in Profile.jsx, the `CAREER_HIGHLIGHTS` project-lookup query and its result handler. Verified via grep that zero `goal` references remain in either file. **Two new click-to-expand affordances added** (Explorer.jsx only — matching the existing evidence bullet→detail pattern exactly): the group-by-project header now shows `description` with an "expand ↓" control that reveals `detail` when populated (state: `expandedProjectDetail`, a `Set` of project keys shared by both new affordances, toggled via `toggleProjectDetail(key)`); the "Project context" box inside an expanded evidence card gets the same treatment, keyed by `` `${r.company} · ${r.project}` `` to match `descByKey`/`detailByKey`'s key format. Neither shows an expand control when that project's `detail` is null — same "don't point an affordance at nothing" rule evidence cards already followed. **The "Project context" box's expand button needed `e.stopPropagation()`**, since it's nested inside the evidence card's own click-to-expand region — without it, clicking "expand ↓" on the project detail would also collapse the outer card. Verified end-to-end by temporarily setting one project's `detail` to a test string, confirming both affordances render and toggle correctly (including that `stopPropagation` actually prevents the outer-card collapse), then clearing it back to `null` — confirmed via `count(detail)` that all 17 rows are null again post-test.
- **Explorer.jsx's Delivery role filter chips no longer come from a hardcoded `FUNC_ROLES` constant** — removed (`const FUNC_ROLES = ["Transformation Lead", "Product Manager", "Project Manager", "Program Manager", "Business Analyst"]`), replaced with `const deliveryRoles = useMemo(() => [...new Set(projects.map(p => p.functionalRole).filter(Boolean))].sort(), [projects])`, derived off the already-fetched `projects` state (no new query). Direct schema check (`select distinct functional_role from projects`) before making the change found the live data actually has **10** distinct values — AI Lead, Business Analyst, Data Lead, Management Consultant, Operations Specialist, People Manager, Product Manager, Program Manager, Project Manager, Salesperson — 6 of which weren't in the old hardcoded list at all (Management Consultant, Salesperson, Operations Specialist, AI Lead, Data Lead, People Manager had no way to be filtered on despite being correctly stored), while `"Transformation Lead"` (the one value some `TARGET_ROLES` presets reference as `deliveryRole`) turned out to not correspond to anything in the live data at all. **Checked, not assumed, that group-by-delivery-role needed no change**: its key function (`groupBy === "delivery" ? (r) => r.functionalRole || "Role-level (no project)" : ...`) already read whatever value was actually on each row — confirmed by grouping and seeing all 10 real values appear as correct groups, unaffected by the fix. This is a deliberately different treatment from Expertise (closed 8-category taxonomy, stays a constant) and Type (fixed 3-value enum, stays a constant) — Delivery role is inherently open-ended and expected to keep growing, so a maintained list will always eventually drift; see requirements.md §2 for the full reasoning. **Known, accepted tradeoff, not fixed here**: `TARGET_ROLES` presets still reference delivery-role strings by exact text (e.g. `"Product Manager"`, `"Transformation Lead"`) — same string-matching fragility already documented for `CAREER_HIGHLIGHTS`/`TESTIMONIALS`. The `"Transformation Lead"`-referencing presets (Business Transformation, AI Transformation Lead) will keep returning zero results until either the preset is corrected or matching real data is logged — this now has a confirmed root cause instead of just being an observed symptom. Verified end-to-end: all 10 chips render alphabetically sorted, clicking one (tested "Salesperson") correctly filters to matching evidence/placeholder cards, and `?groupBy=delivery` correctly buckets all 10 real values plus "Role-level (no project)".
- **Supabase seeded via `scripts/seed.js`** — historical record now (see its own header comment for why it's **not re-runnable as-is**: it parsed COMPANIES/ROLES/PROJECTS/ROLE_EVIDENCE out of Explorer.jsx's source text, which no longer contains that hardcoded data at all following the live-query wiring above). Originally read `COMPANIES`/`ROLES`/`PROJECTS`/`ROLE_EVIDENCE` directly out of Explorer.jsx's source text (a small string-based `extractConst()` parser, not a hand-retyped copy) so the seeded data couldn't drift from what was in the code at the time. Creates one auth user + `profiles` row for Ned as the `owner_id` anchor (via `auth.admin.createUser`). Fractional-year `ROLES.start`/`.end` values (e.g. `2017.83`) converted to real dates via `fractionalYearToDate()` (same encoding as the codebase already used: `.83 ≈ November`); the role with the latest `start` got `end_date: null` (the current/ongoing role — its `end` value was just a far-future placeholder, not a real date to preserve). `roles` has no `sort_order` column (unlike companies/projects/evidence) — role lookups keyed by `(company_id, job_title)` instead.
- **Incident, resolved:** while wiring up the seed script, the Supabase service_role key was briefly written into `.env.example` (tracked by git, not gitignored) instead of `.env.local`. Caught before any commit — confirmed via `git log -p` that it never entered git history. Fixed by moving the real key to `.env.local` and restoring `.env.example` to a placeholder (empty `SUPABASE_SERVICE_ROLE_KEY=` with a comment warning against putting a real value there). Worth double-checking `git status`/`git diff` on any `.env*` file before ever committing, given this file is easy to confuse with the gitignored one.

## Non-negotiable constraints
- **Company = employer only** (UBS, Accenture, JP Morgan). Never make a client (Goldman Sachs, Morgan Stanley) a foreign-keyed `companies` row — they go in `client_name` as plain text. This was a real bug caught before shipping; don't reintroduce it.
- **Role** (job stint / job title) and **Delivery role** (project-specific hat, e.g. Product Manager) are different fields. Never merge them.
- `title` / `bullet` / `detail` on evidence are three distinct things: short label / scannable one-line claim / additive paragraph. `detail` must never just restate `bullet`.
- Grouping by Expertise (multi-valued tags) must never duplicate a full card into every matching group — one full card under its primary (first-listed) tag, a compact cross-reference stub elsewhere.
- `highlighted` is a single boolean, deliberately rare (~4 items). Don't turn it back into a rating scale.
- Filter/group-by dimensions always display in this order: Company → Role → Project → Delivery role → Expertise → Type.
- Search is AND-of-words (every typed word must appear somewhere on the card), never OR. OR-style "match any" search was considered and explicitly rejected — that need is already served by the Company/Role/Project/Delivery role/Expertise/Type chip filters for anything structured; free-text OR would solve a problem the chips already solve. Don't add comma-separated or token-based OR search without a concrete case the chips can't cover.
- RLS requires `auth.uid() = owner_id` for writes — seeding needs the service_role key (server-side/script only, never `VITE_`-prefixed, never committed).

## Commands
- `npm run dev` — local dev server
- `npm run build` — production build
- `node --env-file=.env.local scripts/seed.js` — historical; **not re-runnable as-is** (see the script's own header comment — it parsed hardcoded consts out of Explorer.jsx that no longer exist there since the live-query wiring). `node --env-file=.env.local scripts/backfill-project-years.mjs` — same caveat, already applied.
- No test suite yet.
